package seedu.address.model.transaction;

/**
 * Enumerates the sortable transaction columns that determine the visible order.
 */
public enum TransactionSortKey {
    DIRECTION,
    OTHER_PARTY,
    AMOUNT,
    DESCRIPTION,
    STATUS,
    DATE
}
